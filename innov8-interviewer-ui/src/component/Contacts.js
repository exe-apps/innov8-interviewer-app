import React, { Component } from 'react';

class Contacts extends Component {

    render() {
        return(
            <div>Contacts page is a work-in-progress</div>
        );
    }
}

export default Contacts;